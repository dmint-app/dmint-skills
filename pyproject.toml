[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dmint-skills"
version = "0.1.0"
description = "AI agent policy-authoring skills and system prompt guidance for Dmint"
requires-python = ">=3.10"
readme = "README.md"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
