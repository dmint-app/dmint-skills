# dmint-skills

`dmint-skills` provides AI agent policy-authoring skills and system prompt guidance for Dmint.

## Usage

Use the `dmint-policy-authoring` skill or import `DMINT_POLICY_SYSTEM_PROMPT` for policy generation workflows.

```python
from dmint_skills import DMINT_POLICY_SYSTEM_PROMPT
```
